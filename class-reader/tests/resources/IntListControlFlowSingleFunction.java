import java.util.List;
import java.util.ArrayList;

public class IntListControlFlowSingleFunction {
    void function() {
        List<Integer> list = new ArrayList<>();
        if (list.size() >= 0) {
            list.add(1);
        }
    }
}
